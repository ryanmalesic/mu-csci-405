version: "3.8"
services:
  app:
    image: ryanmalesic/mu-csci-406-lab4-app
    depends_on:
      - mongo
    environment:
      PORT: ${PORT}
    ports:
      - ${PORT}:${PORT}
    restart: always

  mongo:
    image: mongo
    restart: always
